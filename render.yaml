services:
  - type: web
    name: coding-vieb-api
    env: node
    rootDir: server
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    plan: free
    envVars:
      - key: PORT
        value: 4000
      - key: NODE_ENV
        value: production

